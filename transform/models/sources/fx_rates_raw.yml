version: 2

sources:
  - name: bronze
    schema: bronze
    tables:
      - name: fx_rates_raw
        description: >
          USD-based FX rates from Frankfurter v2 pinned to the ECB provider.
          No freshness check: ECB does not publish on weekends or TARGET
          holidays, so any threshold would false-alarm every Saturday.
