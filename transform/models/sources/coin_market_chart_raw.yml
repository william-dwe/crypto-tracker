version: 2

sources:
  - name: bronze
    schema: bronze
    tables:
      - name: coin_market_chart_raw
        description: >
          Daily price/market-cap/volume history from /coins/{id}/market_chart,
          merged on (coin_id, price_ts_ms).
